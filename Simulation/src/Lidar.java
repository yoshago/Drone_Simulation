
public class Lidar {
	int angle;
	int hz;
	int dist;

	public int rayShoot(int x, int y, int droneAngle)
	{
		int sumAng=droneAngle+this.angle
		startX = x;
		startY = y;
		endX   = x + 60 * Math.sin(angle);
		endY   = y + 60 * Math.cos(angle);
		
	}
	
	
}
